<Window x:Class="UI_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI_WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="6*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Column="1" Grid.Row="1">
                <Label Content="UserName" HorizontalAlignment="Center"/>
                <TextBox x:Name="txtUserName" HorizontalAlignment="Center"/>
                <Label Content="Password" HorizontalAlignment="Center"/>
                <TextBox x:Name="txtPassword" HorizontalAlignment="Center"/>
                <Button x:Name="btnLogIn" Content="Log In" HorizontalAlignment="Center"
                  Click="btnLogIn_Click"/>
            </StackPanel>
        </Grid>
</Window>
